<%include ./partials/header.ejs%>
 <div class="profile w-full text-white min-h-screen bg-zinc-800 pt-1">
    <form id="uploadForm" hidden action="/fileupload" method="post" enctype="multipart/form-data">
     <input type="file" name="image">
    </form>
    <div class="profileDetails flex flex-col items-center mt-20">
        <div class="relative">
            <span id="uploadIcon" class="h-8 w-8 flex rounded-full bg-zinc-200 absolute bottom-0 right-0 items-center justify-center ">
                <i class="text-zinc-800 ri-pencil-fill"></i>
            </span i>
            <div class="w-32 h-32 bg-zinc-200 rounded-full overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profileImage%>" alt="">
            </div>
        </div>
        <h1 class="mt-5 font-semibold text-3xl"><%= user.name %></h1>
        <h3 class="text-md"> @<%=user.username %></h3>
        <a href="/edit" class="mt-5 px-7 py-2 bg-zinc-700 rounded-full text-sm font-semibold" >Edit</a>
        <a href="/add" class="px-7 py-2 mt-6 rounded-lg bg-violet-900 text-sm font-semibold text-violet-200">Add new Post</a>
    </div>
    <div class="cards flex flex-wrap gap-10 px-10 mt-10">
        <div class="card">
            <div class="w-52 h-40 bg-zinc-200 rounded-lg overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user?.posts[0]?.image%>" alt="">
            </div>
            <a href="/show/posts" class="inline-block text-xl font-medium mt-3">Your uploaded pins</a>
            <h5 class="text-sm font-medium opacity-60"><%= user.posts.length %> pins</h5>
        </div>
    </div>
 </div>

 <script>
  document.querySelector('#uploadIcon').addEventListener("click", function(){
    document.querySelector('#uploadForm input').click()
  })
  document.querySelector('#uploadForm input').addEventListener("change", function(){
    document.querySelector('#uploadForm').submit()
  })
 </script>

<%include ./partials/footer.ejs%>